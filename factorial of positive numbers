#include <stdio.h>

int main() {
    // Write C code here
    int fact=1,i,num;
    printf("enter the number");
    scanf("%d",&num);
    if(num>0){
        for(i=1;i<=num;i++){
            fact=fact*i;
            
        }
    }
    printf("factorial is %d ",fact);
    return 0;
}
