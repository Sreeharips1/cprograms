#include <stdio.h>

int main() {
    
    int num,rem,rev,s;
    printf("enter the number\n");
    scanf("%d",&num);
    while(num>0){
        rem=num%10;
        s=s+rem;
        num=num/10;
        rev=rev*10+rem;

    }
    printf("%d is the sum and reverse is %d",s,rev);

    return 0;
}
